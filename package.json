{
  "name": "wa-campaigns-platform",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "db:push": "pnpm --filter @wa/database db:push",
    "db:migrate": "pnpm --filter @wa/database migrate:deploy",
    "db:migrate:dev": "pnpm --filter @wa/database migrate:dev",
    "db:generate": "pnpm --filter @wa/database generate",
    "db:studio": "pnpm --filter @wa/database studio",
    "db:seed": "pnpm --filter @wa/database seed",
    "dev:local": "pnpm --parallel --filter=@wa/api --filter=@wa/web run dev",
    "infra:up": "docker compose -f infra/docker-compose.yml up -d",
    "infra:down": "docker compose -f infra/docker-compose.yml down"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0",
    "prettier": "^3.4.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
